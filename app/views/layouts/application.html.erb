<!DOCTYPE html>
<html>
  <head>
    <title>MagmaChat by Magma Labs</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@100;300;400;500;700;900&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen flex flex-col overflow-clip">
    <div class="flex-1 flex flex-col sm:flex-row">
      <main class="flex-1 flex flex-col h-screen top-0 bottom-0 max-h-[100vh] overflow-y-auto bg-gray-600 scroll-smooth">
        <% if content_for?(:header) %>
          <header class="hidden sm:block bg-gradient-to-r from-orange-500 to-red-600 h-12 sticky top-0 z-10 overflow-hidden shadow-xl"><%= yield :header %></header>
        <% end %>
        <div class="flex-1 max-h-screen overflow-auto"><%= yield %></div>
        <% if content_for?(:footer) %>
          <footer class="sticky top-[100vh]"><%= yield :footer %></footer>
        <% end %>
      </main>
      <nav class="order-first sm:w-[20%] bg-gray-800 bg-gradient-to-l from-gray-900 text-gray-300 flex
            sm:flex-col gap-3 py-1 px-2 sm:p-3 items-center sm:items-start h-12 sm:h-screen sm:relative
            sm:overflow-y-auto sm:max-h-screen">
        <%= render "nav" %>
      </nav>
      <% if content_for?(:right) %>
        <aside class="hidden sm:block sm:w-[20%] bg-gray-900"><%= yield :right %></aside>
      <% end %>
    </div>
  </body>
</html>
